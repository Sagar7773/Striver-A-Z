class Solution
{
public:
    int largest(vector<int> &arr, int n)
    {
        int target = arr[0];
        for(int i=1;i<n;i++){
            if(arr[i]>target){
                target = arr[i];
            }
        }
        return target;
    }
};
